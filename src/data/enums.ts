import { UserTypes } from '@/types/user.interface'

export enum LocalItems {
  LANG = 'lng',
  THEME = 'thm',
  ACCESS_TOKEN = 'tkn',
}

export enum Themes {
  LIGHT = 'light',
  DARK = 'dark',
}

export const languageModel = {
  tr: 'tr',
  en: 'gb',
}
